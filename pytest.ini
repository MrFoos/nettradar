[pytest]
# Uten dette finner ikke `pytest` pakken `backend` — pytest legger bare
# testmappa på sys.path, ikke repo-rota. (`python -m pytest` virker fordi
# -m legger til arbeidskatalogen, men CI kaller pytest direkte.)
pythonpath = .
testpaths = tests
